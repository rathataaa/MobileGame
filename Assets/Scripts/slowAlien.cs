using UnityEngine;

class SlowAlien : BaseBrick
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Awake()
    {
        gravityScale = 2f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
